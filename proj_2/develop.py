from tokenization import Tokenizer


t = Tokenizer()

print(t.tokenize(['Now, an additional step for faster processing of the model. You can move the model to the GPU if available, or to the CPU if not.']))